Tecnologias Usadas

React Native + Vite + Mongo DB + Mongo Express + Spring Boot Spring Framework + docker + vagrant


Estrutura

projeto/
├── backend/
│   ├── app.py
│   ├── requirements.txt
│   └── Dockerfile
├── frontend/
│   └── index.html
└── docker-compose.yml
